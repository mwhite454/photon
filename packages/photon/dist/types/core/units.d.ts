/** Get a conversion ratio between a source unit and a destination unit. */
export declare function conversionScale(srcUnitType: string, destUnitType: string): number;
/** Check to see if unit type is a valid Maker.js unit. */
export declare function isValidUnit(tryUnit: string): boolean;
